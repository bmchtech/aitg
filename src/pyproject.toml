[tool.poetry]
name = "aitg_host"
version = "1.4.1"
description = ""
authors = ["xdrie <xdrie@alt.icu>"]

[tool.poetry.dependencies]
python = "^3.6"
aitextgen = "0.5.2"
typer = "^0.3.2"
bottle = "^0.12.19"
loguru = "^0.5.3"
colorama = "^0.4.4"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=1.0.0"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
aitg_host_cli = "aitg_host.cli:main"
aitg_host_srv = "aitg_host.srv:main"
