[tool.poetry]
name = "aitg_host"
version = "0.1.0"
description = ""
authors = ["xdrie <xdrie@alt.icu>"]

[tool.poetry.dependencies]
python = "^3.8"
aitextgen = "^0.3.0"
typer = "^0.3.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
aitg_host_cli = "aitg_host.cli:main"